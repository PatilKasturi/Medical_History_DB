<html lang="en">

   <%- include header %>
   <!-- Main component for a primary marketing message or call to action -->
 <div class="sidenav">
<a href="/profile" >Current</a>
<a href="/family">Family</a>
<a href="/dis" class="active">Disease and allergies</a>
<a href="/acc" >accidents</a>
</div>

<!-- Page content -->
<p style="font-size:200%;">Conditions</p>

<ul class="list-group">
       <% for(var i=0; i<data.length; i++) {%>
           <li class="list-group-item">
             <span>Date _>> </span><%= data[i].date %>
             <br/>
               <span>Condition_name _>> </span><%= data[i].problem_name %>
               <br/>
               <span>Type _>> </span><%= data[i].type %>
               <br/>
               <span>Medication _>> </span><%= data[i].medication %>
           </li>
       <% } %>
   </ul>

      <div class="jumbotron">

        <p>To delete a particular condition type condition_name and hit Remove!!</p>
        <form action="/dis" method="post">

            <% if (message.length > 0) { %>
                                                                                          <div class="alert alert-success col-sm-12"><%= message %></div>
                                                                              <% } %>

                                                                              Date:<br/>
        <input type="date" name="date" placeholder="select the date">
        <h2></h2>
        <input type="radio" name="d" value="allergy"> Allergy <br>
        <input type="radio" name="d" value="disease"> Disease<br>
        <input type="radio" name="d" value="heridity"> Heridity<br>
        <textarea name="name" rows="2" cols="80"  placeholder="condition name"></textarea><br>
        <h2></h2>
        <textarea name="mname" rows="2" cols="80"  placeholder="medication name"></textarea><br>
        <h2></h2>
        <button  type="submit" formaction ="/dis1"  class="btn btn-info">  Remove</button>
        <button type="submit" class="btn btn-info"><i class="icon-hand-right""></i> &nbsp  ADD </button>



      </form>



      </div>
    <%- include footer %>
